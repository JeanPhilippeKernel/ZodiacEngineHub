name: Panzerfaust CI

on:
  pull_request:
    branches: [main, develop]
    paths-ignore:
      - '**.cs'
      - '**.axaml'
      - '**.csproj'
      - '**.manifest'
      - '.github/workflows/**'

jobs:
  commitlint:
    if: github.base_ref != 'main'
    runs-on: ubuntu-latest
    steps:
      - run: echo "No source changes — skipping commitlint"

  ci-passed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No source changes — CI skipped"
